# Repository harness instance

Copy this directory to `.engineering/` in a target repository. This optional
bootstrap writes project scaffolding, not application/runtime source. Fill the
profile before using workflows and leave unknown fields explicit. For root
discovery, follow `AGENTS.merge-snippet.md`; never overwrite an existing root
`AGENTS.md`.
